@page
@model crmHuman.Pages.Leave.LeaveBalanceModel
@using global::System.Globalization
@{
    ViewData["Title"] = Model.TitlePage;
}

<div class="pagetitle">
    <h1>@Model.TitlePage</h1>
    <nav>
        <ol class="breadcrumb">
            <li class="breadcrumb-item"><a href="/">Trang chu</a></li>
            <li class="breadcrumb-item active">Quan ly ngay phep</li>
        </ol>
    </nav>
</div>

<section class="section">
    <div class="row">
        <div class="col-lg-12">
            <div class="card">
                <div class="card-body">
                    @Html.AntiForgeryToken()

                    <form class="row g-3 mb-3" method="get" action="/Leave/LeaveBalance">
                        <div class="col-md-8">
                            <label class="form-label">Tu khoa</label>
                            <input type="text" value="@Model.RequestSearch.Token" name="token" class="form-control" placeholder="Ten, ma, SDT">
                        </div>
                        <div class="col-md-2">
                            <label class="form-label">Hien thi</label>
                            <select name="limit" class="form-select">
                                <option value="10" selected="@(Model.RequestSearch.Limit == 10)">10</option>
                                <option value="20" selected="@(Model.RequestSearch.Limit == 20)">20</option>
                                <option value="50" selected="@(Model.RequestSearch.Limit == 50)">50</option>
                            </select>
                        </div>
                        <div class="col-md-2 d-flex align-items-end">
                            <button type="submit" class="btn btn-primary w-100">Tim kiem</button>
                        </div>
                    </form>

                    <div class="table-responsive">
                        <table class="table table-striped">
                            <thead>
                                <tr>
                                    <th>#</th>
                                    <th>Ma NV</th>
                                    <th>Ho ten</th>
                                    <th>Bo phan</th>
                                    <th>Vi tri</th>
                                    <th class="text-end">Duoc huong</th>
                                    <th class="text-end">Da dung</th>
                                    <th class="text-end">Con lai</th>
                                    <th class="text-end">Nghi benh</th>
                                    <th class="text-end">Nghi rieng</th>
                                    <th class="text-end">Nghi thai san</th>
                                    <th class="text-end">Nghi khong luong</th>
                                    <th class="text-end">Tong nghi</th>
                                    <th class="text-center">Thao tac</th>
                                </tr>
                            </thead>
                            <tbody>
                                @{
                                    var index = 0;
                                }
                                @foreach (var item in Model.LeaveBalanceList.Data)
                                {
                                    index++;
                                    var info = item as dynamic;
                                    var allowedValue = (decimal?)info?.AllowedLeaveDays ?? 0m;
                                    var usedValue = (decimal?)info?.UsedLeaveDays ?? 0m;
                                    var remainingValue = (decimal?)info?.RemainingLeaveDays ?? (allowedValue - usedValue);
                                    var sickValue = (decimal?)info?.UsedSickLeaveDays ?? 0m;
                                    var personalValue = (decimal?)info?.UsedPersonalLeaveDays ?? 0m;
                                    var maternityValue = (decimal?)info?.UsedMaternityLeaveDays ?? 0m;
                                    var unpaidValue = (decimal?)info?.UsedUnpaidLeaveDays ?? 0m;
                                    var totalApprovedValue = (decimal?)info?.TotalApprovedLeaveDays ?? 0m;
                                    var allowedText = allowedValue.ToString("0.##", CultureInfo.InvariantCulture);
                                    var usedText = usedValue.ToString("0.##", CultureInfo.InvariantCulture);
                                    var remainingText = remainingValue.ToString("0.##", CultureInfo.InvariantCulture);
                                    var sickText = sickValue.ToString("0.##", CultureInfo.InvariantCulture);
                                    var personalText = personalValue.ToString("0.##", CultureInfo.InvariantCulture);
                                    var maternityText = maternityValue.ToString("0.##", CultureInfo.InvariantCulture);
                                    var unpaidText = unpaidValue.ToString("0.##", CultureInfo.InvariantCulture);
                                    var totalApprovedText = totalApprovedValue.ToString("0.##", CultureInfo.InvariantCulture);

                                    <tr data-id="@info.Id" data-name="@info.FullName" data-allowed="@allowedText" data-used="@usedText">
                                        <td>@index</td>
                                        <td>@info.UserName</td>
                                        <td>@info.FullName</td>
                                        <td>@info.DepartmentText</td>
                                        <td>@info.PositionText</td>
                                        <td class="text-end">@allowedText</td>
                                        <td class="text-end">@usedText</td>
                                        <td class="text-end">@remainingText</td>
                                        <td class="text-end">@sickText</td>
                                        <td class="text-end">@personalText</td>
                                        <td class="text-end">@maternityText</td>
                                        <td class="text-end">@unpaidText</td>
                                        <td class="text-end">@totalApprovedText</td>
                                        <td class="text-center">
                                            @if (Model.Permision.Edit ?? false)
                                            {
                                                <button type="button" class="btn btn-sm btn-primary" onclick="openLeaveBalanceModal(this)" title="Cap nhat" aria-label="Cap nhat">
                                                    <i class="bi bi-pencil-square" aria-hidden="true"></i>
                                                </button>
                                            }
                                            else
                                            {
                                                <span class="text-muted">--</span>
                                            }
                                            <button type="button" class="btn btn-sm btn-outline-secondary ms-1" onclick="openLeaveDetailsModal(this)" title="Chi tiet" aria-label="Chi tiet">
                                                <i class="bi bi-eye" aria-hidden="true"></i>
                                            </button>
                                        </td>
                                    </tr>
                                }
                                @if (index == 0)
                                {
                                    <tr>
                                        <td colspan="14" class="text-center">Khong co du lieu</td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>

                    @{
                        Html.RenderPartial("formSearch/pagination", Model);
                    }
                </div>
            </div>
        </div>
    </div>
</section>

<div class="modal fade" id="leaveBalanceModal" tabindex="-1">
    <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">Cap nhat ngay phep</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                <input type="hidden" id="leaveBalanceEmployeeId" />
                <div class="mb-3">
                    <label class="form-label">Nhan vien</label>
                    <input type="text" class="form-control" id="leaveBalanceEmployeeName" readonly disabled />
                </div>
                <div class="row">
                    <div class="col-md-6 mb-3">
                        <label class="form-label">So ngay duoc huong</label>
                        <input type="number" step="0.5" min="0" class="form-control" id="allowedLeaveDays" />
                    </div>
                    <div class="col-md-6 mb-3">
                        <label class="form-label">So ngay da dung</label>
                        <input type="number" step="0.5" min="0" class="form-control" id="usedLeaveDays" />
                    </div>
                </div>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Dong</button>
                <button type="button" class="btn btn-primary" onclick="saveLeaveBalance()">Luu</button>
            </div>
        </div>
    </div>
</div>

<div class="modal fade" id="leaveDetailModal" tabindex="-1">
    <div class="modal-dialog modal-lg modal-dialog-centered">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">Chi tiet ngay nghi</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                <div class="mb-3">
                    <label class="form-label">Nhan vien</label>
                    <input type="text" class="form-control" id="leaveDetailEmployeeName" readonly disabled />
                </div>
                <div class="table-responsive">
                    <table class="table table-sm">
                        <thead>
                            <tr>
                                <th>Loai nghi</th>
                                <th>Tu ngay</th>
                                <th>Den ngay</th>
                                <th class="text-end">So ngay</th>
                                <th>Ly do</th>
                                <th>Nguoi duyet hien tai</th>
                                <th>Ngay tao</th>
                                <th class="text-center">Trang thai</th>
                            </tr>
                        </thead>
                        <tbody id="leaveDetailContent">
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>

<script src="/js/pages/leave-balance.js?v=@DateTime.Now.Ticks"></script>
