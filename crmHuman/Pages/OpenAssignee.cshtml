@{

    var tiemObject = Model.resultView;
    var arrayRol = new List<crmHuman.Model.SelectDisplay>()
    {
        new Model.SelectDisplay()
        {
            Code ="1", Name ="Admin"
        },
        new  Model.SelectDisplay()
        {
        Code ="2", Name ="TC"
        },
            new Model.SelectDisplay()
            {
            Code ="9", Name ="HCNS"
            },
        new  Model.SelectDisplay()
        {
        Code ="3", Name ="TL"
        }
        , new Model.SelectDisplay()
        {
            Code ="4", Name ="Marketing"
        },
        new Model.SelectDisplay()
        {
        Code ="6", Name ="Tru?ng CTV"
        }
    };

    var arrayStatus = new List<crmHuman.Model.SelectDisplay>()
        {
            new  Model.SelectDisplay()
            {
            Code ="0", Name ="Kh�ng ho?t d?ng"
            },
            new  Model.SelectDisplay()
            {
            Code ="1", Name ="Ho?t d?ng"
            }
          
        };
}
  <div class="modal-header">
    <h5 class="modal-title">Ph�n case cho th�ng tin </h5>
    <button type="button" class="btn-close"
                    data-bs-dismiss="modal"
                    aria-label="Close"></button>
        </div>

        <div class="modal-body">
             <form id="mainForm2" class="needs-validation" novalidate>
                @if(@tiemObject.Id > 0)
                {
                    <div class="col-12 form-group">
                        <label class="form-label">M� don h�ng</label>
                        <input type="text" readonly value="@tiemObject.Code" name="fullName" id="txtCode" class="form-control" required placeholder="" />
                        <div class="invalid-feedback-cs">
                        </div>

                    </div>
                }

                <div class="col-12 form-group">
                    <label class="form-label">Ph�n cho TC</label>
                    <select name="candidateId" id="cbAssingeeId" class="form-select" aria-label="">
                        <option value="-1">Ch?n ?ng c? vi�n</option>
                        @foreach (var item in Model.allEmployers.Data)
                        {
                            <option value="@item.Id">@item.FullName</option>
                        }
                    </select>
                    <div class="invalid-feedback-cs">
                        Please choose a username.
                    </div>
                </div>
            </form>

        </div>
        <div class="modal-footer">
            <button type="button" class="btn btn-secondary"
                    data-bs-dismiss="modal">
                ��ng
            </button>
    <button type="button" onclick="assingeeOrder(@tiemObject.Id)" class="btn btn-primary">Lưu</button>
        </div>


